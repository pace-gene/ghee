Context:
- Current Date: {{ current_date }}
- User: {{ user_identity }}

Analyze the following activity data and extract key work areas. The raw data (PRs, commits, Linear issues) is already displayed above - do NOT repeat it.

Your task:
1. FIRST: Identify Linear projects or parent tasks that appear frequently (high activity). List these SPECIFICALLY by their exact project/parent names (e.g., "Science CI Optimizations", "Unlock forecasts innovation via Dagster", etc.). These should be the top 3-5 most active projects/parents.
2. THEN: Group the remaining activity into broader themes (e.g., infrastructure maintenance, dependency updates, bug fixes, etc.). These can be vague groupings.
3. Order work areas by activity level (most active first)
4. For specific projects/parents: Describe what was accomplished in that project/parent (2-3 sentences).
5. For vague groupings: Describe the general type of work done (2-3 sentences).
6. DO NOT mention specific PR numbers, issue IDs, or repository names
7. DO NOT list individual items - focus on patterns and themes
8. Keep it concise and analytical

CRITICAL: You must follow this EXACT output format:

# Work Areas

## 1. Work Area Name
Description of what was accomplished in this area (2-3 sentences).

## 2. Work Area Name
Description of what was accomplished in this area (2-3 sentences).

[Continue with list of sections...]

Do NOT include any other lists, text, headers, or sections. Start immediately with "# Work Areas" and use the exact format shown above.

Activity Data:
{{ data_text }}
